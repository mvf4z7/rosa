import theme from '../../views/app/theme';
let palette = theme.getPalette();

let styles = {
    chartWrapper: {
        height: '70%'
    },
    highChart: {
        height: '100%'
    }
};

export default styles;
